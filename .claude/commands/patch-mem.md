Patch claude-mem plugin to remove startup banner noise. Run this after plugin updates if the banner returns.

Execute: `~/.claude/.venv/bin/python ~/.claude/ops/patch_claude_mem_banner.py --all`

Report success or failure.
